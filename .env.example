# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Free Gemini key: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# API key (billed separately from a Claude subscription): https://console.anthropic.com
ANTHROPIC_API_KEY=
